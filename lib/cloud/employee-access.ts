import type {EmployeeAssignment,Project,SiteMessage,WorkTimeEntry} from '../types';
export type EmployeeAccessEvidence='assigned'|'future-assigned'|'past-assigned'|'work-time'|'image-upload'|'message';
export function employeeVisibleProjectIds(employeeId:string,assignments:EmployeeAssignment[],workTimes:WorkTimeEntry[],messages:SiteMessage[],today:string,newUploadedProjectIds:string[]=[]){const ids=new Map<string,Set<EmployeeAccessEvidence>>();const add=(id:string,e:EmployeeAccessEvidence)=>{if(!ids.has(id))ids.set(id,new Set());ids.get(id)!.add(e)};for(const a of assignments.filter(a=>a.employeeId===employeeId)){add(a.projectId,a.date===today?'assigned':a.date>today?'future-assigned':'past-assigned')}for(const w of workTimes.filter(w=>w.employeeId===employeeId))add(w.projectId,'work-time');for(const m of messages.filter(m=>m.createdBy===employeeId&&m.projectId))add(m.projectId!,'message');for(const id of newUploadedProjectIds)add(id,'image-upload');return ids;}
export function filterProjectsForEmployee(projects:Project[],employeeId:string,assignments:EmployeeAssignment[],workTimes:WorkTimeEntry[],messages:SiteMessage[],today:string,uploaded:string[]=[]){const ids=employeeVisibleProjectIds(employeeId,assignments,workTimes,messages,today,uploaded);return projects.filter(p=>ids.has(p.id));}
export function assertEmployeeCanAccessProject(projectId:string,employeeId:string,assignments:EmployeeAssignment[],workTimes:WorkTimeEntry[],messages:SiteMessage[],today:string){if(!employeeVisibleProjectIds(employeeId,assignments,workTimes,messages,today).has(projectId))throw new Error('Zugriff verweigert: Mitarbeiter ist dieser Baustelle nicht zugeordnet.');return true;}
