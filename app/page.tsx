'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation';
export default function LoginPage(){const router=useRouter();const [name,setName]=useState('Gökhan Karayel');const [password,setPassword]=useState('demo');function submit(e:FormEvent){e.preventDefault();localStorage.setItem('kastonia-demo-user',name||'Gökhan Karayel');router.push('/dashboard')}return <div className="loginWrap"><form className="loginCard" onSubmit={submit}><div className="logoMark">K</div><h1>KASTONIA ERP</h1><p>Interne Kalkulation, Kunden, Projekte und Finanzübersicht.</p><label>Name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Passwort<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary" type="submit">ERP öffnen</button><small>Version 0.7 · Funktional</small></form></div>}
