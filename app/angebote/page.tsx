'use client'; import Shell from '../../components/Shell'; import {useStore,eur} from '../../lib/store'; import {useState} from 'react';
export default function Page(){const {state,add}=useStore();const [f,setF]=useState({customer:'',title:'',net:0,status:'Entwurf'});const gross=f.net*1.19;return <Shell><div className="top"><div><span className="eyebrow">VERKAUF</span><h1>Angebote</h1><p>Angebote erfassen und automatisch brutto berechnen.</p></div></div><div className="panel"><div className="formgrid"><select value={f.customer} onChange={e=>setF({...f,customer:e.target.value})}><option value="">Kunde wählen</option>{state.customers.map((x:any)=><option key={x.id}>{x.name}</option>)}</select><input placeholder="Bezeichnung" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><input type="number" placeholder="Netto" value={f.net||''} onChange={e=>setF({...f,net:+e.target.value})}/><select value={f.status} onChange={e=>setF({...f,status:e.target.value})}><option>Entwurf</option><option>Versendet</option><option>Auftrag</option><option>Abgelehnt</option></select><div><b>Brutto: {eur(gross)}</b></div><button onClick={()=>{if(!f.customer||!f.title)return;add('offers',{id:'AG'+Date.now().toString().slice(-6),...f,date:new Date().toISOString().slice(0,10)});setF({customer:'',title:'',net:0,status:'Entwurf'})}}>Angebot speichern</button></div></div><div className="panel"><table><thead><tr><th>Nr.</th><th>Kunde</th><th>Leistung</th><th>Netto</th><th>Status</th></tr></thead><tbody>{state.offers.map((x:any)=><tr key={x.id}><td>{x.id}</td><td>{x.customer}</td><td>{x.title}</td><td>{eur(x.net)}</td><td>{x.status}</td></tr>)}</tbody></table></div></Shell>}
