'use client';import {useState} from 'react';import {getSupabaseBrowserClient} from '../../../lib/cloud/supabase';
export default function ResetPassword(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');async function reset(){const sb=getSupabaseBrowserClient();if(!sb){setMsg('Supabase ist nicht konfiguriert. Bitte Umgebungsvariablen prüfen.');return}const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:(process.env.NEXT_PUBLIC_APP_URL||location.origin)+'/auth/reset-password'});setMsg(error?error.message:'Passwort-Reset wurde ausgelöst.')}return <main><h1>Passwort zurücksetzen</h1><label>E-Mail<input value={email} onChange={e=>setEmail(e.target.value)}/></label><button onClick={reset}>Reset-Link senden</button><p>{msg}</p></main>}
