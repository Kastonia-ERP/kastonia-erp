'use client'; import Shell from '../../components/Shell'; import {useStore} from '../../lib/store'; import {useState} from 'react';
export default function Page(){const {state,add,remove}=useStore();const [f,setF]=useState({name:'',email:'',phone:'',city:''});return <Shell><div className="top"><div><span className="eyebrow">CRM</span><h1>Kunden</h1><p>Kunden dauerhaft auf diesem Gerät speichern.</p></div></div><div className="panel"><h2>Neuer Kunde</h2><div className="formgrid">{['name','email','phone','city'].map(k=><input key={k} placeholder={{name:'Name',email:'E-Mail',phone:'Telefon',city:'Ort'}[k as keyof typeof f]} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button onClick={()=>{if(!f.name)return;add('customers',{id:'K-'+Date.now(),...f,status:'Aktiv'});setF({name:'',email:'',phone:'',city:''})}}>Kunde speichern</button></div></div><div className="panel"><table><thead><tr><th>Kunde</th><th>Kontakt</th><th>Ort</th><th>Status</th><th></th></tr></thead><tbody>{state.customers.map((x:any)=><tr key={x.id}><td><b>{x.name}</b><br/><small>{x.id}</small></td><td>{x.phone}<br/>{x.email}</td><td>{x.city}</td><td>{x.status}</td><td><button className="ghost" onClick={()=>remove('customers',x.id)}>Löschen</button></td></tr>)}</tbody></table></div></Shell>}
