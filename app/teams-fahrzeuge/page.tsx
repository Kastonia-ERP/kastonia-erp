'use client';
import Shell from '../../components/Shell';
import {useStore} from '../../lib/store';
export default function Page(){const {state}=useStore();return <Shell><div className="pageHead"><div><p className="eyebrow">RESSOURCEN</p><h1>Team & Fahrzeuge</h1><p>Monteure, Verfügbarkeit, Fuhrpark und Werkzeugbestand.</p></div></div><section className="twoCol"><article className="panel"><h2>Monteure</h2>{state.teamMembers.map(member=><div className="alertRow" key={member.id}><b>{member.name}</b><span>{member.phone} · {member.staffRole} · {member.availability}{member.sick?' · Krank':''}<br/>{member.skills.join(', ')}</span></div>)}</article><article className="panel"><h2>Firmenfahrzeuge</h2>{state.vehicles.map(vehicle=><div className="alertRow" key={vehicle.id}><b>{vehicle.licensePlate}</b><span>{vehicle.type} · TÜV {new Date(vehicle.inspectionDue).toLocaleDateString('de-DE')} · Service {new Date(vehicle.serviceDue).toLocaleDateString('de-DE')}<br/>{vehicle.tools.join(', ')} · Anhänger {vehicle.trailer?'Ja':'Nein'}</span></div>)}</article></section></Shell>}
